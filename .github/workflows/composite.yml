on: [push]

jobs:
  composite:
    runs-on: self-hosted
    name: Composite test
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: OWNER/bootcamp/composite@SHA
        with:
          who-to-greet: 'GORBLIN'
      - run: echo random-number "$RANDOM_NUMBER"
        shell: bash
        env:
          RANDOM_NUMBER: ${{ steps.foo.outputs.random-number }}
